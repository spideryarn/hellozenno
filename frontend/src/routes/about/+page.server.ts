import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
  return {
    // You can add any server-side data that might be needed for the about page here
  };
};